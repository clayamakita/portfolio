<button aria-label="Menu" aria-controls="primary-navigation" aria-expanded="false" class="hamburger-button">
    <svg class="hamburger" viewBox="0 0 100 100" width="250" fill="var(--button-color)">
        <rect class="line first-line" width="80" height="10" x="10" y="25" rx="5"></rect>
        <rect class="line second-line" width="80" height="10" x="10" y="45" rx="5"></rect>
        <rect class="line third-line" width="80" height="10" x="10" y="65" rx="5"></rect>
    </svg>
</button>

<style lang="scss">
    .hamburger-button {
        display: none;
    }
    @media (width < 35em) {
        .hamburger-button {
            display: block;
            background-color: transparent;
            border: 0;
            position: absolute;
            width: 2.5rem;
            aspect-ratio: 1;
            right: 1.6rem;
            z-index: 9999;

            .hamburger {
                width: 100%;
                height: 100%;
                fill: var(--light-color-1);
            }

            .line {
                transition: rotate 100ms ease-in, y 100ms ease-in 100ms, opacity 0ms 100ms;
                transform-origin: center;
            }
            &[aria-expanded="true"] .line {
                transition: y 100ms ease-in, rotate 100ms ease-in 100ms, opacity 0ms 100ms;
            }
            &[aria-expanded="true"] :is(.first-line, .third-line) {
                y: 45;
            }
            &[aria-expanded="true"] .first-line {
                rotate: 45deg;
            }
            &[aria-expanded="true"] .second-line {
                opacity: 0;
            }
            &[aria-expanded="true"] .third-line {
                rotate: -45deg;
            }
        }
    }
</style>